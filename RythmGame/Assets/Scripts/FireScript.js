#pragma strict
var attack: GameObject;
function Start () {

}

/*function Update () {
	if (Input.GetButtonDown(KeyCode.Space)) {
			fireAttack = Instantiate(attack, transform.position, transform.rotation);
			fireAttack.velocity = transform.TransformDirection(Vector3.forward * 10);
		}
}*/